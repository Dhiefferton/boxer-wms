const express = require('express'); const { zenErpGet } = require('../poller'); const router = express.Router(); router.get('/outgoinglist/:id', async (req, res) => { try { const resposta = await zenErpGet(`/material/outgoingList/${req.params.id}`); res.json({ id: resposta.data.id, status: resposta.data.status }); } catch (erro) { res.status(502).json({ erro: erro.message, detalhe: erro?.response?.data }); } }); module.exports = router;
