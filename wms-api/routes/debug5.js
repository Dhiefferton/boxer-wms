const express = require('express'); const { zenErpGet } = require('../poller'); const router = express.Router(); router.get('/stock-sample', async (req, res) => { try { const sku = req.query.sku || '99023'; const resposta = await zenErpGet('/material/stock', { q: `productPacking.product.code==${sku}`, max: 3 }); const lista = Array.isArray(resposta.data) ? resposta.data : resposta.data?.data || []; res.json(lista); } catch (erro) { res.status(502).json({ erro: erro.message, detalhe: erro?.response?.data }); } }); module.exports = router;
