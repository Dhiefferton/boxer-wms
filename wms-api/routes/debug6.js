const express = require('express'); const { zenErpGet } = require('../poller'); const router = express.Router(); router.get('/pickingorders-fila-real', async (req, res) => { try { const resposta = await zenErpGet('/material/pickingOrder', { q: 'reservation.status==APPROVED;pickingProfile.code==EXPEDICAO' }); const lista = Array.isArray(resposta.data) ? resposta.data : resposta.data?.data || []; res.json({ total: lista.length, ids: lista.map(p => p.id), tem39994: lista.some(p => p.id === 39994) }); } catch (erro) { res.status(502).json({ erro: erro.message, detalhe: erro?.response?.data }); } }); module.exports = router;
